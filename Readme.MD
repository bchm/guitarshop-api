# Jack's Guitar Shop, now on Docker!

I added Docker to the API Java app from @wimmelstein 

***To run this application on docker use these commands***

``` mvn clean install
``` mvn clean package
``` docker-compose build
``` docker-compose up